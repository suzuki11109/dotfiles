#!/bin/sh

case $BLOCK_BUTTON in
	1) pavucontrol ;;
	2) pamixer -t ;;
	#4) pamixer -i 5 ;;
	#5) pamixer -d 5 ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

if [ $(pamixer --get-mute) = true ]; then
	echo "[Muted]"
	exit
fi

vol="$(pamixer --get-volume)"
# [ $? -ne 0 ] && exit $?

echo "[VOL $vol]"
